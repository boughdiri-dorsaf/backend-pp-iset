/* const express = require("express");
const router = express.Router();
const demandeMasterController=require('../../controllers/evenementiel/demandeMaster')


router.post("/", demandeMasterController);

router.get("/", demandeMasterController);

router.get("/:id", demandeMasterController);

router.patch("/", demandeMasterController);

router.delete("/:id", demandeMasterController);

module.exports = router;
 */