/* const express = require("express");
const router = express.Router();
const domaineController=require('../../controllers/evenementiel/domaine')


router.post("/", domaineController);

router.get("/", domaineController);

router.get("/:id", domaineController);

router.patch("/", domaineController);

router.delete("/:id", domaineController);

module.exports = router;
 */